import Comments from "src/modules/Comments";

const CommentsPage = () => {
    return (
        <div className="flex items-center justify-center bg-commentsPage bg-top bg-repeat-y">
            <Comments />
        </div>
    );
};

export default CommentsPage;
